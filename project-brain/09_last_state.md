# последнее состояние проекта

Этот файл нужен, чтобы Codex быстро понимал, где мы остановились.

## Текущий фокус

главный MVP-сценарий и снижение когнитивной нагрузки:

клиент создает заказ → водитель видит заказ → водитель принимает заказ → статус меняется → заказ завершается.

## 2026-07-08 (Claude) — аудит: 3 бага исправлено, все 9 smoke-тестов зелёные

- Аудит всего приложения: прогнаны все 9 smoke-тестов (dispatch, realtime, referrals, driver-billing, verification, documents, auth-delivery, geo-messaging, registration-no-sms) + ревью свежего кода.
- Исправлено: (1) событие `driver_chat` без snapshot роняло realtime-статус всех клиентов в «polling» при каждом сообщении чата — ранний return в `onMessage`; (2) сохранение аватара без backend показывало сырую ошибку API — мягкий локальный фолбэк; (3) `geo-messaging-smoke-test` был зашит на старый «эконом фикс 120» — обновлён под «село 120 / межсельские 30 ₽/км» + village-кейс.
- Известные edge (не чинил, низкий приоритет): адрес без запятой («Малояз школа») не совпадёт с «Малояз, …» по селу → посчитается как межсельская; `myRevenueBy` в /admin/stats не учитывает платежи без paidAt.

## 2026-07-08 (Claude) — аватар водителя + «кто приедет» у клиента + общий чат водителей

- **Аватар водителя**: карточка «Фото профиля» на вкладке «Аккаунт» водителя (`DriverAvatarCard`, галерея/камера через expo-image-picker, квадратная обрезка, dataUri ≤ ~0.9 МБ). Бэк: `PATCH /drivers/:id/avatar` (canAccessDriver, валидация `data:image/(jpeg|png|webp)`, мусор → 400), `driver.avatar` хранится на профиле; при принятии заказа уходит клиенту в `order.driver.avatar`. Клиент видит **кто приедет** на экране заказа: фото (кругом) + имя, машина, рейтинг, госномер (имя/машина/номер уже были). Без бэка карточка честно предупреждает, фото держится локально.
- **Общий чат водителей**: `DriverChatScreen` (тёмный ScreenHero «Чат водителей · Смены, дороги и взаимовыручка — без клиентов», пузыри с аватарами/именами, свои справа зелёным, poll 6с + мгновенная отправка). Вход — карточка «Чат водителей» на главной водителя (`DriverHomePage` → `onOpenDriverChat` → route `DriverChat`). Бэк: `db.driverChat` (cap 500), `GET /driver-chat` (водители+админ, последние 200), `POST /driver-chat` (text ≤ 600, identity из профиля водителя; админ пишет как «Диспетчер»), лёгкий realtime-event `driver_chat` без snapshot.
- Затронуты: `scripts/mvp-backend.mjs`, `src/services/apiClient.ts`, `src/state/AppState.tsx` (тип avatar), `src/components/DriverAvatarCard.tsx` (новый), `src/screens/DriverChatScreen.tsx` (новый), `src/components/PostRegistrationMenu(.styles)`, `src/screens/OrderStatusScreen.tsx`, `src/screens/DashboardScreen.tsx`, `src/navigation/*`.
- Проверено: typecheck и `test:realtime` зелёные; бэк живьём (curl): avatar сохраняется, не-image → 400, чат POST/GET (админ = «Диспетчер»); UI живьём (Playwright): кнопка чата на главной, экран чата с композером, карточка «Фото профиля» в Аккаунте. Деплой в gh-pages.

## 2026-07-08 (Claude) — тариф владельца: по селу 120 ₽, межсельские 30 ₽/км

- Уточнение правила: **внутри одного села — фикс 120 ₽** (любой тариф, note «по селу — фикс»), **между сёлами — 1 км = 30 ₽**. Село определяется по первой части адреса до запятой (`extractSettlement`: «Малояз, Советская 12» → «малояз»), сравнение регистронезависимое. Константа `VILLAGE_FLAT_PRICE = 120` (клиент `orderFlow.routeEstimate.ts` + бэк `estimateRouteFare` — симметрично). Опции (кресло/багаж) добавляются поверх фикса. Водительская оценка — та же (вся сумма его).
- Проверено (бэк живьём): Малояз→Малояз 120 ₽ «по селу — фикс» (эконом и комфорт), Малояз→Аркаул 26.5 км → 800 ₽; водительская роль — 800 ₽. `test:dispatch` зелёный (Guard-адреса — разные «сёла», контракт 380 ₽ в силе), typecheck зелёный. Деплой в gh-pages.

## 2026-07-08 (Claude) — тариф владельца: 1 км = 30 ₽

- Расчёт стоимости поездки переведён на правило владельца **1 км = 30 ₽**: `getFareRate` (клиент `orderFlow.routeEstimate.ts` + бэк `estimateRouteFare`) теперь возвращает `{ base: 0, perKm: 30, perMin: 0 }` для всех тарифов и ролей (водительская оценка дохода та же — вся сумма его). Убран противоречащий спецкейс «эконом фикс 120 ₽» на обеих сторонах; остановки удорожают через +1.8 км каждая. **Минималки тарифов сохранены** (эконом 120 ₽ и т.д.) — защита коротких поездок; итог округляется до 10 ₽.
- `dispatch-smoke-test` обновлён под новый контракт цены ((9 + 2×1.8) км × 30 → 380 ₽ для неизвестных адресов).
- Проверено: бэк живьём — 26.5 км → 800 ₽ (~30 ₽/км), 3.2 км → минималка 120 ₽; `test:dispatch`, `test:referrals` зелёные; UI — CTA «Вызвать за 120 ₽» на коротком маршруте. Typecheck зелёный. Деплой в gh-pages.

## 2026-07-08 (Claude) — админка владельца: аналитика, цены, чёрный список, чат поддержки

- **Подробная статистика** (`GET /admin/stats` + `AdminStatsBoard` в разделе «Статистика»): новые пользователи за час/день/неделю/месяц/год/всего + колонки регистраций по месяцам (12 мес); деньги — «мой доход» (оплата доступа) всего/месяц/неделя/день, траты клиентов (= доход водителей, платят напрямую), средний чек, колонки моего дохода и оборота поездок по месяцам; заказы — всего/активные/завершены/отменены + колонки за 14 дней + **география «из какого села заказывают»** (из pickup-адресов); водители — на линии/допущены/в ЧС/средний рейтинг, тарифы (день/PRO), статусы, марки авто. Графики — самописные View-бары/полосы (`AdminOwnerTools`), без зависимостей. Возраст водителей не выводим — поле не собирается при регистрации.
- **Цены доступа меняются из админки** (раздел «Расчеты» → `AdminPricingCard`): смена (день) и Партнёр PRO (месяц). Бэк: `db.settings` + `GET /admin/settings` + `PATCH /admin/pricing`; `applyPricingSettings` мутирует runtime-тарифы — списание, `/driver-payments/settings` и экран доступа водителя подхватывают мгновенно; realtime-уведомление «Тарифы обновлены».
- **Чёрный список** (новый раздел меню, `AdminBlacklistCard`): блокировка водителя (по driver-id или user-id — снимается с линии, `blacklisted`-блокер закрывает ленту) и клиента (по id/телефону/почте — `POST /orders` отвечает 403). `db.blacklist`, `GET/POST /admin/blacklist`, `DELETE /admin/blacklist/:id` (разблокировка).
- **Чат поддержки** (раздел «Поддержка» → `AdminSupportChat`): список обращений со статусами (Ждёт/Отвечен/Закрыт), открытие треда, пузыри сообщений, ответ **от лица поддержки** — `appendSupportMessage` получил admin-ветку `asSupport` (одно support-сообщение без автоответа, статус `answered`; статус добавлен в normalize и тип `SupportThread`).
- Затронуты: `scripts/mvp-backend.mjs`, `src/services/apiClient.ts` (типы + 6 admin-функций, `asSupport`), `src/components/admin/AdminOwnerTools.tsx` (новый), `src/screens/AdminPanelScreen.tsx` (интеграция секций, пункт «Чёрный список»), `src/state/AppState.tsx` (тип).
- Проверено: typecheck зелёный; `test:realtime`, `test:dispatch` зелёные; живьём с бэкендом (Playwright): вход → цены 120→150 сохранены → водитель добавлен в ЧС («снят с линии, лента закрыта») → чат открыт. Curl: stats/settings/pricing/blacklist add+remove. Деплой в gh-pages. QA-нюанс: в production-сборке API-URL берётся из EXPO_PUBLIC_API_URL, локально тестировать с `window.process.env` override.

## 2026-07-07 (Claude) — удобная пошаговая логика регистрации

- **Пошаговая валидация**: «Продолжить» больше не пропускает вслепую — проверяются только поля текущего шага; ошибки подсвечиваются прямо под полями (мягкий красный + короткое сообщение: «Заполните это поле», «Похоже на опечатку — проверьте формат почты», «Минимум 8 символов», «ИНН — ровно 12 цифр», «Год числом: 1950–NNNN», «Для такси нужен стаж от 3 лет»). Красное появляется только после попытки продолжить и гаснет сразу при исправлении поля. Тёплая панель «Почти готово» подсказывает, что осталось.
- **Финальный шаг**: при ошибках в анкете панель показывает список + **чипы «Исправить: Контакт/Пароль/Детали»** — тап открывает нужный шаг с уже подсвеченными проблемными полями (`jumpToFix`). Отдельная подсказка про неотмеченные согласия.
- **Чипы шагов в hero кликабельны назад** (пройденные шаги, `aria` «Шаг N: …»), автоскролл к началу при смене шага.
- Рефакторинг `utils/validation.ts`: новая `validateRegistrationFields(role, values) → FieldErrors` (id поля → сообщение) + `hasMissingConsent`; старая `validateRegistration` собирается из по-полевых ошибок (единые формулировки). `FieldInput` получил проп `error` (красная рамка + текст вместо helper).
- Затронуты: `src/utils/validation.ts`, `src/components/FieldInput.tsx`, `src/screens/RegistrationScreen.tsx`.
- Проверено живьём (Playwright, 390px): пустой контакт → 4 инлайн-ошибки + подсказка; кривая почта → своя ошибка; валидный контакт → переход; короткий пароль → блок; финал без согласий → панель; чип «Шаг 2» → возврат на Контакт. Typecheck зелёный. Деплой в gh-pages.

## 2026-07-07 (Claude) — регистрация в фирменном премиум-языке

- `RegistrationScreen` переоформлен под язык приложения (логика шагов не тронута):
  - **тёмный hero** (#0A1411, glow, лаймовый back-chip, водяной знак «KINETIX») с заголовком/подзаголовком активного шага, иконкой шага (Роль→Navigation, Контакт→UserRound, Пароль→KeyRound, Детали→Car, Готово→ClipboardCheck), **лаймовой прогресс-линией** и чипами шагов (✓-done лайм, активный — кольцо с подписью, будущие — номера; подпись только у активного, чтобы влезали 5 шагов на 390px);
  - **роль-карточки** как на Welcome: белая премиум (radius 20, тень #0B2F25/0.05/20) ↔ активная глубокий зелёный #0B7C48 с лаймовым чеком; внизу фирменный рельс маршрута (точка→линия→пин); иконки в сквирклах;
  - **секции анкеты** — иконка секции в светло-зелёном сквиркле (identity→FileText, legal→ShieldCheck, vehicle→Car, business→Building2, payments→Wallet);
  - **шаг «Готово»** — сводка строками `SummaryRow` (Роль/Телефон/Почта с иконками) на подложке #F7FBF8;
  - **CTA**: зелёная (radius 14, minHeight 56, шеврон; на финале «Создать аккаунт»), под ней тихий ряд «Назад · Уже есть аккаунт»; убраны вертикальный дубль списка шагов и старый брендовый хедер.
- Затронут только `src/screens/RegistrationScreen.tsx`.
- Проверено: typecheck зелёный; все 5 шагов просмотрены живьём (Playwright, 390px). Деплой в gh-pages.

## 2026-07-07 (Claude) — встроенный навигатор водителя (как у Яндекс Go)

- Новый полноэкранный **навигатор** для водителя: `src/screens/NavigatorScreen.tsx` + `src/components/DriverNavigatorMap.tsx`. Внутри iframe — Leaflet + OSM-тайлы + маршрут по дорогам (OSRM `steps=true`), живой GPS (`watchPosition`, `allow="geolocation"`), поворот маркера-стрелки по heading, режим следования (drag отключает, кнопка «прицел» возвращает), пере-построение маршрута при уходе с него (>60 м ×3). Без GPS (демо/desktop) — автоматическая **симуляция движения** по маршруту (чип «Демо GPS»). Связь iframe↔RN: `postMessage` (state наружу, `recenter` внутрь).
- Весь продуктовый UI — RN-оверлеи в тёмном премиум-стиле: баннер следующего манёвра (лаймовая иконка поворота, «260 м», «Поверните направо», улица), ETA-панель (мин/км/время прибытия/цель), чипы фаз «Подача»/«Поездка» (remount iframe по фазе), выход. Русские инструкции манёвров (turn/fork/merge/roundabout/uturn/arrive).
- **Graceful-деградация для слабого интернета**: без Leaflet/тайлов навигация живёт (сетка-плейсхолдер «Карта появится при подключении», баннер и ETA работают); без OSRM — прямая линия до цели. Ретик состояния каждые 1.5с (единичный GPS-фикс не оставляет баннер пустым).
- Вход: кнопка «Навигатор» (графит+лайм) на экране заказа у водителя (активные статусы; фаза: `started`→Поездка, иначе Подача). Прямой вход для демо/QA: `/?screen=navigator` — `getInitialWebState` отрефакторен (`mapEntryScreen`), поддержка `?screen=` для всех entry-путей (admin/invite/order/navigator) — работает на статичном хостинге, где SPA-пути дают 404.
- Native (APK): экран-заглушка «доступен в web-версии»; позже тот же HTML — в `react-native-webview`.
- Затронуты: `src/screens/NavigatorScreen.tsx` (новый), `src/components/DriverNavigatorMap.tsx` (новый), `src/navigation/types.ts`, `src/navigation/AppNavigator.tsx`, `src/screens/OrderStatusScreen.tsx`.
- Проверено: typecheck зелёный; Playwright — сценарий с granted-геолокацией (баннер манёвра), без геолокации (симуляция+«Демо GPS»+ETA), переключение фаз. Кнопку «Навигатор» на экране заказа живьём не прогонял (нужен активный заказ водителя) — код тривиален. Деплой в gh-pages.

## 2026-07-07 (Claude) — нативные пуши: конфиг-хук FCM (код уже готов)

- Ревизия нативных пушей: инфраструктура **code-complete** и не была задокументирована как готовая. Клиент — `src/services/pushNotifications.ts` (запрос разрешения, нативный токен FCM/APNs, категория `driver_order_offer` с кнопками «Принять/Пропустить`»), вызывается на логине (`AppState` ~566). Бэк — `POST /push-tokens` + хранение, `sendPushToDriver/User/Admins`, реальная отправка `sendFcmPush` (через `firebase-admin`) и `sendApnsPush` (через `apn`); диспетчеризация на событиях заказа (создание/назначение/статус/отмена). Зависимости `firebase-admin`, `apn`, `expo-notifications`, `expo-device` — в `package.json`; плагин `expo-notifications` — в `app.config.js`.
- Добавлен недостающий конфиг-хук: `android.googleServicesFile = process.env.GOOGLE_SERVICES_JSON || undefined` — включает FCM в APK, не ломая web/dev без файла.
- **Что нужно от владельца, чтобы пуши «полетели» (не код):** Firebase-проект → service account JSON на бэк (`MVP_FIREBASE_SERVICE_ACCOUNT_JSON` или `_PATH` / `GOOGLE_APPLICATION_CREDENTIALS`); `google-services.json` в сборку (EAS-секрет `GOOGLE_SERVICES_JSON`); для iOS — APNs-ключ (`MVP_APNS_KEY_PATH/KEY_ID/TEAM_ID`). Плюс реальная APK/dev-сборка (в вебе и Expo Go пуши не приходят). Без конфигурации бэк логирует «FCM skipped» и работает по realtime-каналу.

## 2026-07-07 (Claude) — реквизиты водителя клиенту + чистый статус на экране заказа

- **Реквизиты водителя для оплаты клиентом** (клиент платит за поездку напрямую водителю). Поле «Карта или счёт для оплаты от клиента» (`payoutAccount`) собиралось при регистрации, но не доходило до клиента — цепочка достроена: бэк сохраняет `payoutAccount` на профиле водителя (`makeDriverFromUser` при регистрации + `POST /drivers` у админа), прокидывает в клиентский `order.driver` (участник заказа при принятии/назначении). Клиент видит реквизиты в блоке «Оплата» → «Оплатить»: строка «Реквизиты» теперь показывает реальные реквизиты водителя (было — захардкоженная фейковая карта `2200 24•• •••• 4011`); если не заданы — «Уточните у водителя». Тип `OrderParticipant.payoutAccount` добавлен.
- Экран статуса заказа: техтексты сетевых ошибок (`Failed to fetch`, коды, timeout) больше не показываются клиенту — `friendlyRealtimeMessage` заменяет их на «Обновляется автоматически» / «Данные обновляются в реальном времени».
- Тест: `realtime-smoke-test` — создаваемому водителю задаётся `payoutAccount`, проверяется, что клиент видит его в `order.driver.payoutAccount` после назначения. `test:realtime` зелёный.
- Затронуты: `scripts/mvp-backend.mjs`, `scripts/realtime-smoke-test.mjs`, `src/state/AppState.tsx`, `src/screens/OrderStatusScreen.tsx`.
- Проверено: typecheck зелёный; `test:realtime` зелёный; панель оплаты и чистый статус просмотрены живьём (Playwright). Деплой в gh-pages.

## 2026-07-07 (Claude) — ручная оплата доступа (карта владельца) + брендовый фолбэк карты поездки

- Оплата доступа водителя переведена на явную **ручную модель без эквайринга** (клиент платит за поездку напрямую водителю; водитель платит владельцу за смену/месяц переводом на карту):
  - `SubscriptionScreen` — новый блок «Оплата доступа — на карту»: сумма выбранного тарифа, **номер карты владельца с кнопкой «Копировать»** (на web — `navigator.clipboard`), держатель, шаги 1-2-3, приписка «С поездок процента нет — клиент платит вам напрямую». Убраны артефакты эквайринга (кнопки «Открыть оплату»/«Проверить оплату», `confirmationUrl`, sync); удалены неиспользуемые импорты/метод `syncDriverSubscriptionPayment`. Если карта не настроена (демо без бэка) — мягкий фолбэк «Реквизиты карты покажет владелец сервиса».
  - Бэкенд `GET /driver-payments/settings` теперь отдаёт полный `cardNumber` (форматирование по 4), `cardHolder`, обе суммы `dailyAmount`/`monthlyAmount` и ручную `instructions`. Новый хелпер `formatPaymentCardNumber`, env `PAYMENT_CARD_HOLDER` (в паре с `PAYMENT_CARD_NUMBER`). Модель денег: смена (daily) открывается сразу по тапу (оплата переводом, лог платежей — запись владельца); Партнёр PRO (monthly) — заявка `awaiting_manual_transfer` → активация админом (`PATCH /drivers/:id/access`, уже было). Реальный прод — `MVP_PAYMENT_PROVIDER_MODE=manual`.
  - `apiClient.DriverPaymentSettings` расширен (`cardNumber`, `cardHolder`, `dailyAmount`, `monthlyAmount`).
- `TripDriverMap`: фолбэк, когда Leaflet/тайлы недоступны (офлайн/CSP/демо), больше не «Карта недоступна — проверьте интернет», а **брендовая SVG-схема маршрута** (сетка, линия старт→машина→пин, «Маршрут поездки», адреса `pickup → destination`, «Салаватский район · с. Малояз»). Адреса экранируются (`escapeHtml`).
- Затронуты: `scripts/mvp-backend.mjs`, `src/services/apiClient.ts`, `src/screens/SubscriptionScreen.tsx`, `src/components/TripDriverMap.tsx`.
- Проверено: typecheck зелёный; endpoint settings в manual-режиме отдаёт полный номер карты (curl); экран доступа и фолбэк карты просмотрены живьём (Playwright). Деплой в gh-pages. Для боевой ручной оплаты запускать бэк с `MVP_PAYMENT_PROVIDER_MODE=manual`, `PAYMENT_CARD_NUMBER`, `PAYMENT_CARD_HOLDER`.

## 2026-07-07 (Claude) — отмена поездки клиентом + уведомление водителю

- Клиент может отменить активную поездку с экрана статуса (`OrderStatusScreen`): кнопка «Отменить поездку» (спокойный красный `#B23B32`, только пока заказ не завершён/не отменён) с двухшаговым подтверждением («Точно отменить поездку? · Да, отменить / Нет»), вызывает `updateOrderStatus(order.id, 'cancelled')`. После отмены статус-карта показывает иконку `Ban`, «Поездка отменена» и текст (клиенту — «Вы отменили…», водителю — «Клиент отменил поездку»); прогресс-бар и кнопка отмены скрываются, кнопка водителя гаснет в «Заказ отменён».
- Бэкенд (`scripts/mvp-backend.mjs`): переход в `cancelled` и возврат бонуса уже были; добавлена метка `cancelled/canceled → «поездка отменена»` в `getOrderStatusLabel`; при отмене именно клиентом (`getSessionRole === 'client'`) водителю уходит отдельное понятное уведомление «Клиент отменил поездку» (kind `order_cancelled`) вместо обезличенного «Статус заказа обновлён». Доставка — существующими `sendPushToDriver` + `broadcastRealtime('order_status', {notification, order})` с полным snapshot, так что у водителя заказ обновляется в реальном времени.
- Тест: `scripts/realtime-smoke-test.mjs` расширен сценарием — клиент отменяет назначенный заказ, второй realtime-клиент получает `order.status==='cancelled'` и уведомление с заголовком «Клиент отменил поездку», адресованное назначенному водителю. `test:realtime` зелёный.
- Затронуты: `scripts/mvp-backend.mjs`, `src/screens/OrderStatusScreen.tsx`, `scripts/realtime-smoke-test.mjs`.
- Проверено: typecheck зелёный; `test:realtime` зелёный; клиентский поток (демо-клиент: создать заказ → «Отменить поездку» → подтвердить → «Поездка отменена») просмотрен живьём (Playwright). Важно: на github.io-демо бэкенда нет, поэтому кросс-девайс уведомление водителю там не увидеть — отмена отражается локально; полный realtime — при запущенном `npm run backend` + `EXPO_PUBLIC_API_URL`. Деплой в gh-pages.

## 2026-07-07 (Claude) — полноэкранная «Лента заказов» в премиум-язык

- Полноэкранная лента водителя (`OrderFlowScreen`, ветка `isDriverRole`, открывается из превью-ленты кнопкой «Открыть полный экран») приведена к тому же премиум-языку, что и `DriverFeedPreview`: светлый заголовок заменён на тёмный `ScreenHero` (иконка маршрута, лаймовая кнопка «назад», подзаголовок «Заказы рядом — расстояние, адрес и цена»); статус-строка очищена от технического `realtimeMessage` («API request failed: 404») на «Обновляется автоматически» с иконкой `RefreshCw`; статус-блок и тумблеры сортировки вынесены в отдельные премиум-карточки (радиус 18, мягкая тень `#0B2F25`/0.05).
- `CompactOrderCard` переоформлена под премиум-макет (`card-mock.html`): скруглённая карточка (радиус 18) с левым зелёным акцент-баром, бейдж услуги с иконкой (Car/Package), крупная цена (22, `#12382C`), рельс маршрута (кружок старта → линия → квадрат-пин), чипы дистанции/оплаты с иконками (MapPin/Wallet), info-кнопка 48×48 и CTA «Принять заказ» (радиус 14). Проп `isDelivery` для выбора иконки.
- Пустое состояние `KinetixEmptyState` заменено на кинематографичный `FeedRadarEmpty` (радар-кольца + Route-иконка в ядре, «Ищем заказы рядом»); locked-вариант (нет допуска) — статичный со `ShieldCheck` и «Доступ закрыт».
- Затронуты: `src/screens/OrderFlowScreen.tsx` (+ импорты `MapPin`/`RefreshCw`, убран неиспользуемый `KinetixEmptyState`), `src/screens/OrderFlowScreen.styles.ts`.
- Проверено: typecheck зелёный; полноэкранная лента и превью-лента просмотрены живьём (Playwright, demo-driver, reducedMotion off) — тёмный hero, radar-empty и статус совпадают с превью. Деплой в gh-pages.

## 2026-07-07 (Claude) — редизайн «Ленты заказов» + навигация/тема

- «Лента заказов» водителя (`DriverFeedPreview` в `PostRegistrationMenu.tsx`) полностью переоформлена в премиум-язык: тёмный hero остался, ниже — живой статус-блок (пульсирующая точка `FeedPulseDot` + счётчик «N рядом»), премиум-карточки заказов с визуализацией маршрута (точка старта → линия → пин), бейдж услуги (Такси/Доставка), крупная цена, чип дистанции, зелёная CTA «Принять заказ», разворачиваемые детали, stagger-появление. Пустое состояние — кинематографичное с радар-кольцами `FeedEmptyState` («Ищем заказы рядом»). Новые стили `feed*` в `PostRegistrationMenu.styles.ts`; хелпер `splitFeedRoute`.
- Кнопка «Назад» добавлена туда, где её не было: driver section-страницы (Автомобиль и др.), клиент «О приложении», auth-экраны (VerifyPhone/VerifyEmail/PasswordReset). ☰ добавлен на ScreenHero-экраны (Рефералы, История, Подписка, Документы, Адрес) — ведёт на главную.
- Welcome по умолчанию в светлой теме (`useState('light')`). Регистрация приведена к бренду Kinetix (белая стрелка на зелёном).
- Экран загрузки (web) — единственный boot-splash с aurora/крупным wordmark; self-updater по build-id.txt (свежая версия при обновлении).
- Проверено: typecheck зелёный; лента и карточка просмотрены (Playwright + HTML-макет). Деплой в gh-pages.

## 2026-07-04 (Claude) — кинематографичные анимации: запуск и заказ

- Экран запуска: в `dist/index.html` пост-экспортным шагом (`scripts/inject-web-splash.mjs`) вставлен статичный тёмный boot-splash (графит #0A1411 + лайм-пин с пульсом), чтобы первый кадр web совпадал с миром приложения, а не был белой вспышкой. React-сплэш `SalavatSplash` в `App.tsx` перекрашен в ту же ночную палитру (glow-блобы, лайм-раскрытие маршрута), лежит слоем поверх навигатора и бесшовно перетекает в тёмный hero Welcome. boot-слой гаснет, как только React-сплэш смонтирован (`#kinetix-boot`). Уважает `prefers-reduced-motion` (короткий статичный кадр).
- Новый скрипт `npm run build:web` = expo export + inject-web-splash.
- Экран заказа (`OrderFlowScreen`): вход-хореография — карта-сцена оседает лёгким zoom-out, шит выезжает снизу по drawer-кривой, бар с CTA всплывает следом (играет один раз, `stagger`). CTA при отправке заказа морфится во вращающийся `LoaderCircle`. Цена и «Итого» тикают одометром (`TickerText`), чекмарк бонусов появляется через `PopIn`.
- В `KinetixUI` добавлены переиспользуемые примитивы `TickerText` (одометр чисел) и `PopIn` (scale 0.6→1), в токены — кривая `kinetixEasing.drawer` (iOS Ionic).
- Проверено: typecheck зелёный; сплэш и вход заказа просмотрены живьём (Playwright): boot → route-reveal → Welcome hero без стыка, сцена заказа отыгрывает вход и вёрстка цела. Задеплоено в gh-pages.

## 2026-07-03 (Claude) — надёжность реферальной системы + оплата бонусами

- Прогон логики рефералки нашёл 7 дыр, все закрыты в `scripts/mvp-backend.mjs`:
  двойная выплата (авто-награда + повторное админское rewarded) — `rewardReferral` no-op для уже rewarded, дедуп `creditWallet` для рефералов по источнику, а не тексту причины;
  сгорание бонусов — `calculateBonusBalance` переписан на честный FIFO (списания гасят старейшие кредиты, истёкший остаток сгорает и «потраченное» не воскресает);
  отмена заказа — новый `refundOrderBonus` возвращает списанные бонусы (идемпотентно через дедуп `trip_refund`), вызывается из статус-роута;
  подсчёт заказов приглашённого водителя — единые `countDriverCompletedOrders`/`countClientCompletedOrders`, тест/спорные заказы исключены;
  статус реферала больше не даунгрейдится (ниже порога — только audit `progress`);
  `PATCH /admin/referrals/:id/status` и `POST /auth/register` переведены на `mutateDb` (уникальность email/phone и выплата — в одной критической секции).
- UI: в ценнике `OrderFlowScreen` появился тумблер «Оплатить бонусами» (клиент с положительным балансом): показывает «Спишем N ₽ бонусами», цена и CTA пересчитываются, `useBonus` уходит на бэк через `addOrder`. Демо-клиенту засеяно 60 ₽, чтобы фича была видна на github.io.
- `scripts/referral-smoke-test.mjs` расширен: защита от двойной выплаты, возврат при отмене, FIFO-сгорание (истёкший 200₽-кредит, погашенный частично списанием).
- Проверено: `test:referrals`, `test:dispatch`, `test:realtime`, `typecheck` — зелёные; тумблер просмотрен живьём (Playwright, demo-client). Деплой в gh-pages выполнен.

## 2026-06-18 (Codex) — Фаза 0/1/2 по плану Claude

- Фаза 0: в `06_project_decisions.md` закреплён стандарт «не одна длинная страница»: короткие сфокусированные экраны, общий step-rail, `slide` вперёд, `fade` на завершение, вторичные действия в отдельных routes/экранах.
- Фаза 1: биллинг водителя переведён на два режима `daily | monthly`: день 100 ₽ / 24 ч и Партнёр PRO 2 490 ₽ / 30 дней. `src/data/subscription.ts` больше не содержит `commission`, 7/5/3 и service-share проценты. Backend оставляет старые service-share endpoints инертными: суммы 0, статус `not_applicable`, legacy `commission` нормализуется в `daily`.
- UI: `SubscriptionScreen` показывает две карточки День/Месяц; водительский dashboard показывает доступ и срок, а не «к сверке»/комиссию.
- Фаза 2: `RegistrationScreen` разбит на реальные короткие шаги `Роль → Контакт → Детали → Готово` с animated step pane. Клиентская главная очищена до двух крупных входов «Такси» / «Доставка»; история, дом, поддержка и профиль вынесены в отдельные экраны/меню.
- Проверено: `npm run typecheck`, `npm run test:driver-billing`, `npm run test:registration-no-sms` — зелёные. Expo web поднят локально на `http://localhost:19006`.

## 2026-06-15 (Claude) — идемпотентность принятия заказа + аудит статус-переходов

- Аудит: forward-only валидация статусов и защита от гонки двух водителей УЖЕ реализованы (`ORDER_STATUS_RANK` + `isAllowedOrderStatusTransition`; accept под сериализованным `mutateDb` с проверкой `order.driver?.id` и `['created','searching']`). Долг из прошлой версии этого файла устарел — внедрять заново было нечего (подтверждено записью в `07_bugs.md`).
- Фикс: повторное «принять» тем же водителем теперь no-op `200` вместо вводящего в заблуждение `409` (`mvp-backend.mjs`, обработчик `assign`). Биллинг на повторный `completed` идемпотентен (`settleOrderPayment` пересчитывает дневную долю, не инкрементирует) — отдельной правки не понадобилось.
- Решение записано в `06_project_decisions.md` (п.10), баг — в `07_bugs.md`. Проверено: `npm run test:dispatch`, `test:realtime` — зелёные.
- Процесс: решение принято после read-only консультации с Codex (диалог в треде), но адаптировано под реальные статусы проекта, а не под его generic-таблицу `new/accepted/in_progress`.

## 2026-06-15 (Codex) — чистка Obsidian-памяти для Claude + Codex

- Восстановлены потерянные первые буквы в базовых заметках `project-brain` и разделе экономии лимитов `AGENTS.md`.
- В `00_main.md` добавлены Obsidian-ссылки на `25_team_protocol` и `17_codex_roles`, чтобы командный протокол был виден из главной заметки.
- Код приложения не менялся.

## 2026-06-15 (Claude) — оформлена команда Claude + Codex

- Создан `CLAUDE.md` — инструкция Claude как участника команды, симметрична `AGENTS.md` (зона Claude: архитектура, бизнес-логика, надёжность бэка, дизайн/UX, ревью).
- Создан `project-brain/25_team_protocol.md` — единый регламент передачи: Codex черновик/инфра → Claude доводка/логика/дизайн → перекрёстное ревью (`codex exec review` ⇄ `/code-review`) → фиксация.
- В `17_codex_roles.md` добавлен блок про передачу Codex↔Claude.
- В `AGENTS.md` `CLAUDE.md` и `25_team_protocol.md` внесены в список «что читать каждую задачу».
- Проверка: `npm run typecheck` зелёный (exit 0). Блокер из `codex_learning_log` (5 июня: `AdminPanelScreen` → `DriverSubscriptionPayment`) уже устранён — тип живёт в `src/data/subscription.ts`, импорты корректны.

## 2026-06-15 (Claude) — надёжность и вес бандла

- Справочник домов Салавата вынесен из стартового web-бандла: 21.5 МБ → 4.1 МБ (web грузит `public/salavatDistrictHouses.json` лениво, натив — отдельным чанком).
- Стили вынесены из трёх монолитов (OrderFlow / PostRegistrationMenu / AdminPanel) в отдельные `*.styles.ts`.
- Backend: записи состояния сделаны атомарными и сериализованными; все 5 мутирующих маршрутов заказа (`assign`, `payment`, `status`, `offer`, `service-share`) переведены на `mutateDb` — убраны гонки read-modify-write. Проверено 10 smoke-тестами.
- Открытый долг: статус-переходы пока без forward-only валидации (см. `07_bugs.md`); единое JSON/jsonb-хранилище ещё не нормализовано.

## 2026-07-02 (Claude) — premium-редизайн обеих ролей + платная смена + надёжные документы

- Premium-язык (тёмный hero #0A1411 + карточки радиус 16–20, линии `rgba(11,47,37,0.10)`, мягкие тени, лайм-акценты) доведён на всех клиентских экранах (главная v9, заказы, аккаунт, о приложении, поддержка, история, рефералы, адрес, подписка) и на всех водительских (главная-смена, лента, доход, профиль, документы). Общие компоненты: `ScreenHero` (стек-экраны, с «назад»), `ClientPageHeader`/`DriverPageHero` (вкладки, с ☰). На мобильном у водителя верхняя панель и профиль-сайдбар скрыты — hero вплотную к верху, как у клиента; wide-режим с сайдбаром сохранён.
- Анимации: `StaggerView` (появление) + `PressableScale` (нажатие) на клиентских и водительских экранах.
- Бизнес-логика «платная смена»: самозанятый без активного доступа видит CTA «Оплатить и выйти на линию» → экран оплаты (Subscription). 24-часовое авто-завершение дневного доступа уже было на бэке (`normalizeDriverSubscriptionStatus`). `driverNeedsPayment` в `DashboardScreen`, `requiresPayment` в `driverLine`.
- Надёжность документов: бэкенд валидирует/декодирует пачку целиком до записи на диск (атомарный отказ, без осиротевших файлов), дубликаты kind в пачке отклоняются, заменённые файлы чистятся фоном (`removeDriverDocumentFiles`). Клиент: `ApiHttpError` со статусом в `apiClient`; `submitDriverDocuments` возвращает `{ok, message}` — отказ сервера (4xx) больше не маскируется под офлайн и не подделывает локальный статус «на проверке»; экран проверяет лимит 5 МБ при выборе файла и сохраняет выбор при отказе для повтора. Smoke-тест `test:documents` расширен (атомарность, отсутствие орфанов, чистка замен) — зелёный.
- Лёгкий realtime-канал координат сделан: `PATCH /drivers/:id/location` + `broadcastDriverLocation` (событие `driver_location` с одной точкой, без snapshot), клиент точечно патчит водителя в `AppState`, периодический GPS-пинг в `DashboardScreen` переведён на него. Smoke: `test:realtime` расширен событием `driver_location`; `test:dispatch` очищен от протухших проверок (PIN, триал, service-share-отказы) под текущую модель «без процента» — зелёный.
- Subscription/Verify/PasswordReset переведены на premium-токены; вход в админку — по личному паролю владельца (демо-фолбэк + дефолт бэка), кнопка «Демо-админ» удалена.
- Надёжность создания заказа (по анализу слабых мест): (1) таймер эксклюзивного оффера и GET `/orders`,`/realtime/snapshot` больше не пишут устаревшие копии базы — освобождение офферов идёт через `mutateDb` на свежей копии (раньше непринятый оффер откатывал все изменения за 30 сек); (2) идемпотентный `clientRequestId` стабилен между повторами и меняется при правке маршрута — ретрай после таймаута не плодит дубликаты; (3) отказ сервера (`ApiHttpError`) не маскируется под офлайн и не создаёт фантомный локальный заказ, причина показывается на экране; (4) снапшот не стирает локальные офлайн-заказы (merge по clientRequestId); (5) серверный пересчёт цены для всех клиентских тарифов (client total игнорируется, кроме доверенных ролей); (6) `stops`/`scheduledAt` сохраняются на сервере, надбавка за остановки в серверной оценке = клиентской (40₽/ост. economy); (7) запрет pickup==destination (клиент+сервер); (8) лимит 2 активных заказа на клиента (409). Dispatch-смоук расширен этими инвариантами — зелёный.
- Остаточный долг по заказам: пуши шлются внутри mutateDb (медленный провайдер тормозит очередь записей) — вынести после стабилизации.
- Открытый долг: live-GPS между устройствами виден только с запущенным бэкендом (`EXPO_PUBLIC_API_URL`); github.io-демо остаётся бессерверным.

## что уже сделано

- Создана база project-brain для Obsidian.
- Создан AGENTS.md.
- Создан TODO.md.
- Создан DEVELOPMENT_PLAN.md.
- база проекта сохранена в GitHub.

## что делать дальше

Следующий шаг по плану:
- Фаза 3: упростить этапы заказа (`OrderFlowScreen`) — такси быстро, доставка пошагово, общий step-rail, убрать простыню.
- Добавить аккуратный переход в чат с водителем (`slide + fade` / shared-element ощущение).
- После Фазы 2/3 запускать Codex-трек: разбить `DriverDocumentsScreen`, `AdminPanelScreen`, `ReferralScreen` на отдельные экраны-шаги по новому стандарту.

## важно

Текущий пользователь разрешил продолжать план и вносить изменения.

## 2026-09-09 — фоновый GPS, диспетчерский ввод, безопасность

Сделано:
- Фоновый GPS водителя (expo-task-manager + expo-location): координаты идут при погашенном экране, только на смене; постоянное уведомление на Android; тексты разрешений и UIBackgroundModes в app.config.js. НЕ ПРОВЕРЕНО НА ЖЕЛЕЗЕ — нужен APK на реальном телефоне.
- Диспетчерский ввод: экран «Заказ по телефону» (вход из админки), заказ создаётся как обычный клиентский (role client, status searching) с телефоном позвонившего, попадает в диспетчеризацию и предлагается ближайшему водителю. Цену считает сервер.
- Привязка карточки водителя, заведённой парком заранее, к аккаунту по телефону.
- Убран личный пароль владельца из клиентского кода (был в публичном бандле и на gh-pages).

Тесты: 12/12 зелёные, добавлены test:dispatcher-order и test:driver-linking.

Следующее:
- Собрать APK и проверить фоновый GPS в поле.
- Экран диспетчера для роли park_admin (сейчас вход только из админки).
- PostgreSQL вместо JSON-файла до реального запуска.
- Аватарки водителей вынести из базы в файлы.
