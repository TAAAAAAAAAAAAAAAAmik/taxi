Read AGENTS.md, TODO.md, DEVELOPMENT_PLAN.md and all files inside project-brain.

The project is already started. Do not create a new project from scratch.

Work with the current project structure only.

First, analyze the main MVP scenario:

client creates an order -> driver sees the order -> driver accepts the order -> order status changes -> order is completed.

Do not change code yet.

Give me an analysis:
1. which files are responsible for creating an order;
2. which files are responsible for the driver dashboard;
3. where orders are stored;
4. how order status is currently changed;
5. what is missing for a working MVP;
6. which 3 small tasks should be done first.

Do not edit files until I approve the plan.
